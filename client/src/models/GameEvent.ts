enum GameEvent {
    ITEMS = 'game-items',
    CREATE = 'game-create',
    CREATED = 'game-created',
    UPDATE = 'game-update',
    UPDATED = 'game-updated',
    REMOVE = 'game-remove',
    REMOVED = 'game-removed',
}

export default GameEvent;
